	<%= include header %>
	<div class="container">
		<div class="text-center">
			<h1>Our Roster</h1>
			<br>
			<div class="main_roster">
			<% users.forEach( function( player , index ){ %>
				<%= include author-card %>
			<% }); %>
			</div>
		</div>
	</div>
	<%= include footer %>